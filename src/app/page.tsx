import { Login } from "@/screens";
import Image from "next/image";

export default function Home() {
  return (
    <main className="min-h-screen flex items-center w-screen h-screen mx-auto">
      <Login />
    </main>
  );
}
